# .env file

# Database connection string (Prisma uses this to connect to your DB)
DATABASE_URL="postgresql://user:password@localhost:5432/lovecraftian_ecommerce?schema=public"

# Secret key for JWT authentication (make sure to change this for production)
JWT_SECRET="your-secure-jwt-secret-key"

# Other possible variables you might want to add:
# - Email service credentials
# - API keys for third-party services
# - Server port
PORT=3000






#First Version is above, the more Full and Complete Second Version is Below:






# .env

# Database configuration for Prisma (PostgreSQL in this case)
DATABASE_URL="postgresql://user:password@localhost:5432/squid_ecommerce?schema=public"

# JWT secret for authentication tokens
JWT_SECRET="your_jwt_secret_here"  # This should be a strong secret, ideally stored securely.

# Port the Express app will listen on
PORT=5000

# Configuration for the email service, e.g., SendGrid, Mailgun, etc. (if you need it)
EMAIL_HOST="smtp.mailtrap.io"
EMAIL_PORT="2525"
EMAIL_USERNAME="your_smtp_username"
EMAIL_PASSWORD="your_smtp_password"

# Additional API configurations or secrets (e.g., payment gateway keys)
PAYMENT_GATEWAY_API_KEY="your_payment_gateway_api_key"
